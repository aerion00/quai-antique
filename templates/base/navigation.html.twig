{% block navigationHome %}
<div class="container">

    <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
        <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
            <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"></use></svg>
            <span class="fs-4">Quai Antique</span>
        </a>

        <ul class="nav nav-pills">

            {# LIENS HOMEPAGE #}
            <li class="nav-item"><a href="{{ path('app_home') }}" class="nav-link" aria-current="page">Acceuil</a></li>
            <li class="nav-item"><a href="{{ path('app_booking') }}" class="nav-link">Réservation</a></li>
            <li class="nav-item"><a href="{{ path('app_card') }}" class="nav-link">Notre carte</a></li>

            {# LIENS PAGES EXTERNES #}
            {% if app.request.attributes.get('_route') == 'app_home' %}
            <li class="nav-item"><a href="#menu" class="nav-link">Notre menus</a></li>
            {% endif %}

            {# LIENS USER #}
            {% if app.user %}

                <li class="nav-item"><a href="{{ path('app_account') }}" class="btn btn-outline-success me-2">Mon compte</a></li>
                <li class="nav-item"><a href="{{ path('app_logout') }}" class="btn btn-outline-secondary">Se deconnecter</a></li>

            {% else %}

                <li class="nav-item"><a href="{{ path('app_register') }}" class="btn btn-outline-success me-2">Inscription</a></li>
                <li class="nav-item"><a href="{{ path('app_login') }}" class="btn btn-outline-secondary">Se connecter</a></li>

            {% endif %}

        </ul>
    </header>


</div>
{% endblock %}